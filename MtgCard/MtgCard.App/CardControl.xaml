<UserControl
    x:Class="MtgCard.App.CardControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MtgCard.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid  Width="100" Height="100" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Image x:Name="image">
            
        </Image>
        <StackPanel Orientation="Horizontal" 
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right">
            <Button x:Name="addCopy" Click="addCopy_Click">+</Button>
            <Button x:Name="removeCopy" Click="removeCopy_Click">-</Button>
            <TextBlock x:Name="copies">2</TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
